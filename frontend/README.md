# OpsBoard Frontend

Angular 21 SPA for OpsBoard.

## Local commands

```bash
npm install
npm start
npm run build
npm run test
```

The app expects the Laravel API on the paired local port: `4200 -> 8000`, `4201 -> 8001`.
